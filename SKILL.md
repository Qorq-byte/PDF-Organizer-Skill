---
name: pdf-organizer
description: 自动读取、理解、分类、摘要和整理 PDF 文档的通用 AI Agent Skill。支持学术论文、教材讲义、课件、报告、合同、会议材料、通知、说明书、电子书等 PDF。能够提取元数据与正文、生成多级摘要、识别主题和文档类型、提取关键词与实体、建立目录与索引、检测重复或相似 PDF，并按照用户规则输出结构化整理结果。遇到扫描件时优先识别可搜索文本，必要时使用 OCR；对低质量或不确定内容明确标注，不凭空补全。
---

# PDF Organizer

## 1. Skill 目标

把用户杂乱的 PDF 文件转换成可搜索、可理解、可归档的结构化知识资料。

核心流程：

```text
原始 PDF
   ↓
文档识别
   ↓
正文/结构提取
   ↓
文档分类
   ↓
关键信息提取
   ↓
摘要生成
   ↓
关键词与标签
   ↓
目录/章节索引
   ↓
重复与相似文档检测
   ↓
结构化整理
```

核心目标：

> 让用户不必逐份打开 PDF，也能快速知道“这是什么、讲了什么、哪些最重要、应该放在哪里”。

## 2. 支持的 PDF 类型

自动识别常见类型：

- 学术论文
- 课程教材
- 教师讲义
- PPT 导出 PDF
- 实验报告
- 调研报告
- 项目文档
- 技术文档
- 产品说明书
- 会议纪要
- 会议材料
- 通知公告
- 合同与协议
- 电子书
- 新闻/资料汇编
- 简历/申请材料
- 发票/表单/清单类 PDF
- 扫描 PDF
- 混合型 PDF

无法可靠判断类型时使用 `unknown`，不要强行分类。

## 3. 输入方式

支持单个 PDF、多个 PDF、整个 PDF 文件夹、压缩包中的 PDF、用户提供的 PDF 路径，以及 Agent 文件工具导入的 PDF。

批量输入时，先建立文档清单，再统一分析。

## 4. 文档处理原则

处理顺序：

```text
① 文件识别
② 文本提取
③ 页面/章节结构识别
④ 元数据提取
⑤ 内容分类
⑥ 摘要
⑦ 关键词
⑧ 主题标签
⑨ 文档关系分析
⑩ 输出整理结果
```

优先使用可搜索文本。扫描件在文本层缺失时再进行视觉识别/OCR。识别质量过低时标记 `extraction_confidence: low`，不得猜测缺失文字。

## 5. 文档元数据提取

尽可能提取：

```json
{
  "filename": "",
  "title": "",
  "author": [],
  "organization": "",
  "date": "",
  "year": "",
  "language": "",
  "page_count": 0,
  "document_type": "",
  "subject": [],
  "keywords": [],
  "source": "",
  "version": "",
  "extraction_confidence": "high"
}
```

缺失信息使用 `unknown` / `not found`，不要编造。

## 6. 自动分类

采用多级分类：

```text
一级分类
    ↓
二级主题
    ↓
具体标签
```

例如：

```text
学习资料
└── 英语
    └── CET-6
        ├── 阅读
        ├── 听力
        └── 写作
```

或：

```text
科研
└── 计算机
    └── 机器学习
        ├── Transformer
        ├── LLM
        └── RAG
```

分类树根据实际内容动态生成，不强制套用固定结构。

## 7. 文档类型判断

根据标题、作者、摘要、目录、章节结构、页眉页脚、关键词、版式和语义判断文档类型，并输出类型、置信度和原因。

## 8. PDF 简略概括

默认支持三级摘要。

### 一句话摘要

1～2 句话直接说明核心内容，不只是复述标题。

### 快速摘要

默认 3～7 条，突出主要观点、重要数据、关键结论、核心方法或重要要求。

### 详细摘要

按 PDF 的真实章节结构生成，不强行套用学术论文模板。

## 9. 重点信息提取

自动识别：

```text
定义
结论
观点
数字
日期
人物
机构
地点
技术名词
专业术语
政策要求
任务要求
风险提示
行动项
```

尽量保留 `文件名 + 页码` 作为来源。

## 10. 关键词提取

每份 PDF 输出 5～15 个核心关键词，必要时增加专业术语、人物/机构、技术名词和主题词。不要无意义堆砌大量关键词。

## 11. 核心结论提取

优先回答：

> 这份 PDF 最值得记住的是什么？

根据文档类型分别提取研究发现、主要贡献、主要发现、关键数据、建议、适用对象、时间、地点、要求、功能、注意事项等。

## 12. 页面/章节级摘要

长 PDF 优先使用章节级摘要；需要时进一步输出页码范围与摘要。重点关注摘要、目录、结论、数据表、图表、关键章节以及用户指定关键词。

## 13. 目录识别

优先读取原始目录，并标记 `toc_source: original`。如果原始目录不存在但能够可靠识别章节，生成逻辑目录并标记 `toc_source: inferred`，不可将推测目录伪装成原目录。

## 14. 文档关系分析

批量 PDF 中识别：

- 同主题
- 同作者
- 同课程
- 同项目
- 同时间段
- 同版本
- 补充材料
- 前后续版本
- 内容高度相关

## 15. 重复 PDF 检测

区分：

```text
exact         完全重复
near_duplicate 高度相似但有少量变化
related       同主题但并非重复
```

同主题不等于重复。

## 16. 版本识别

根据文件名、日期、版本号、正文变化和修改说明识别草稿、修订版、最终版等，并建立版本树。无法确定最新版本时明确标记不确定。

## 17. 文件重命名建议

默认只提出建议，不直接修改原文件。建议格式：

```text
[类别]_[主题]_[年份]_[版本]
```

例如：

```text
[课程资料]_大学英语六级阅读真题_2026.pdf
```

只有用户明确授权时才执行实际重命名。

## 18. 文件夹整理建议

默认生成建议结构，例如：

```text
PDF/
├── 学习资料/
│   ├── CET-4/
│   └── CET-6/
├── 论文/
│   ├── AI/
│   └── Computer Science/
├── 工作/
│   ├── 报告/
│   └── 会议/
└── 其他/
```

未经明确授权不得移动或删除原文件。

## 19. 智能总索引

批量 PDF 默认生成：

```text
| 文件 | 类型 | 主题 | 年份 | 页数 | 摘要 | 标签 |
|------|------|------|------|------|------|------|
```

需要时同时输出 CSV/JSON 等结构化索引。

## 20. 文档卡片

每份 PDF 默认生成：

```text
━━━━━━━━━━━━━━━━━━
📄 文档卡片
━━━━━━━━━━━━━━━━━━

文件：example.pdf
类型：学术论文
主题：Large Language Models
页数：18

一句话摘要：……

核心内容：
• …
• …
• …

关键词：#LLM #RAG #AI

核心结论：
1. …
2. …
3. …

重要页码：P.4 / P.9 / P.15

推荐归档：AI论文
```

## 21. 用户问题驱动分析

用户可以询问：

- 这份 PDF 讲了什么？
- 哪几页最重要？
- 找出关于某主题的内容。
- 提取所有关键数据。
- 整理考试重点。
- 总结结论、建议或行动项。

默认优先基于当前 PDF 原文回答；文档中没有明确依据时说明“未在文档中找到”。除非用户明确要求，不用外部知识替代文档内容。

## 22. 主题聚合

用户可以要求从多个 PDF 中汇总某一主题：

```text
多个 PDF
 ↓
主题筛选
 ↓
提取相关内容
 ↓
去重
 ↓
按主题合并
 ↓
给出文件名与页码来源
```

## 23. 学习资料模式

对课程/考试资料重点提取：

```text
考试重点
知识点
定义
公式
概念
章节重点
易混淆概念
老师强调内容
课后习题
```

不因为识别为学习资料就擅自生成答案。

## 24. 会议/工作文档模式

重点提取：

```text
会议主题
时间
参与人
讨论问题
主要决定
待办事项
负责人
截止日期
```

## 25. 合同/正式文件模式

重点提取：

```text
主体
日期
金额
期限
义务
权利
限制
违约
终止条件
重要条款
```

摘要不是法律意见；存在歧义时以正式文件原文为准。

## 26. 不确定性与事实约束

必须做到：

- 不编造缺失信息。
- 尽量提供文件名和页码。
- 明确区分“原文明确”和“根据内容推断”。
- OCR 或解析不可靠时说明不确定性。

示例：

```text
原文明确：……
根据内容推断：……
```

## 27. 批量整理工作流

用户要求整理整个文件夹时：

```text
扫描 PDF
   ↓
建立文件清单
   ↓
提取元数据
   ↓
识别文档类型
   ↓
识别主题
   ↓
生成摘要
   ↓
提取关键词
   ↓
检测重复/版本
   ↓
建立分类
   ↓
生成总索引
   ↓
输出整理报告
```

默认不删除文件。发现疑似重复时先报告候选重复项。

## 28. 自动整理模式

只有用户明确要求“直接帮我整理文件”等操作请求时才执行文件移动/重命名。

执行前记录：

```text
原路径
新路径
操作类型
原因
```

执行后输出变更日志。

## 29. 输出模式

### Quick

```text
类型
一句话摘要
3个重点
5个关键词
```

### Standard

默认模式：

```text
文档卡片
+
摘要
+
关键词
+
核心结论
+
分类建议
```

### Detailed

```text
文档结构
章节摘要
重点信息
数据
结论
页码
关键词
文档关系
```

