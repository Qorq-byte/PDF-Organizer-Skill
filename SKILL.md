---
name: pdf-organizer
description: 自动读取、理解、分类、摘要和整理 PDF 文档的通用 AI Agent Skill。支持学术论文、教材讲义、课件、报告、合同、会议材料、通知、说明书、电子书等 PDF。能够提取元数据与正文、生成多级摘要、识别主题和文档类型、提取关键词与实体、建立目录与索引、检测重复或相似 PDF，并按照用户规则输出结构化整理结果。遇到扫描件时优先识别可搜索文本，必要时使用 OCR；对低质量或不确定内容明确标注，不凭空补全。
---

# PDF Organizer

## 1. Skill 目标

把用户杂乱的 PDF 文件转换成可搜索、可理解、可归档的结构化知识资料。

核心流程：

```text
原始 PDF
   ↓
文档识别
   ↓
正文/结构提取
   ↓
文档分类
   ↓
关键信息提取
   ↓
摘要生成
   ↓
关键词与标签
   ↓
目录/章节索引
   ↓
重复与相似文档检测
   ↓
结构化整理
```

核心目标：

> 让用户不必逐份打开 PDF，也能快速知道“这是什么、讲了什么、哪些最重要、应该放在哪里”。

## 2. 支持的 PDF 类型

自动识别常见类型：

- 学术论文
- 课程教材
- 教师讲义
- PPT 导出 PDF
- 实验报告
- 调研报告
- 项目文档
- 技术文档
- 产品说明书
- 会议纪要
- 会议材料
- 通知公告
- 合同与协议
- 电子书
- 新闻/资料汇编
- 简历/申请材料
- 发票/表单/清单类 PDF
- 扫描 PDF
- 混合型 PDF

无法可靠判断类型时使用 `unknown`，不要强行分类。

## 3. 输入方式

支持单个 PDF、多个 PDF、整个 PDF 文件夹、压缩包中的 PDF、用户提供的 PDF 路径，以及 Agent 文件工具导入的 PDF。

批量输入时，先建立文档清单，再统一分析。

## 4. 文档处理原则

处理顺序：

```text
① 文件识别
② 文本提取
③ 页面/章节结构识别
④ 元数据提取
⑤ 内容分类
⑥ 摘要
⑦ 关键词
⑧ 主题标签
⑨ 文档关系分析
⑩ 输出整理结果
```

优先使用可搜索文本。扫描件在文本层缺失时再进行视觉识别/OCR。识别质量过低时标记 `extraction_confidence: low`，不得猜测缺失文字。

## 5. 文档元数据提取

尽可能提取：

```json
{
  "filename": "",
  "title": "",
  "author": [],
  "organization": "",
  "date": "",
  "year": "",
  "language": "",
  "page_count": 0,
  "document_type": "",
  "subject": [],
  "keywords": [],
  "source": "",
  "version": "",
  "extraction_confidence": "high"
}
```

缺失信息使用 `unknown` / `not found`，不要编造。

## 6. 自动分类

采用多级分类：

```text
一级分类
    ↓
二级主题
    ↓
具体标签
```

例如：

```text
学习资料
└── 英语
    └── CET-6
        ├── 阅读
        ├── 听力
        └── 写作
```

或：

```text
科研
└── 计算机
    └── 机器学习
        ├── Transformer
        ├── LLM
        └── RAG
```

分类树根据实际内容动态生成，不强制套用固定结构。

## 7. 文档类型判断

根据标题、作者、摘要、目录、章节结构、页眉页脚、关键词、版式和语义判断文档类型，并输出类型、置信度和原因。

## 8. PDF 简略概括

默认支持三级摘要。

### 一句话摘要

1～2 句话直接说明核心内容，不只是复述标题。

### 快速摘要

默认 3～7 条，突出主要观点、重要数据、关键结论、核心方法或重要要求。

### 详细摘要

按 PDF 的真实章节结构生成，不强行套用学术论文模板。

## 9. 重点信息提取

自动识别：

```text
定义
结论
观点
数字
日期
人物
机构
地点
技术名词
专业术语
政策要求
任务要求
风险提示
行动项
```

尽量保留 `文件名 + 页码` 作为来源。

## 10. 关键词提取

每份 PDF 输出 5～15 个核心关键词，必要时增加专业术语、人物/机构、技术名词和主题词。不要无意义堆砌大量关键词。

## 11. 核心结论提取

优先回答：

> 这份 PDF 最值得记住的是什么？

根据文档类型分别提取研究发现、主要贡献、主要发现、关键数据、建议、适用对象、时间、地点、要求、功能、注意事项等。

## 12. 页面/章节级摘要

长 PDF 优先使用章节级摘要；需要时进一步输出页码范围与摘要。重点关注摘要、目录、结论、数据表、图表、关键章节以及用户指定关键词。

## 13. 目录识别

优先读取原始目录，并标记 `toc_source: original`。如果原始目录不存在但能够可靠识别章节，生成逻辑目录并标记 `toc_source: inferred`，不可将推测目录伪装成原目录。

## 14. 文档关系分析

批量 PDF 中识别：

- 同主题
- 同作者
- 同课程
- 同项目
- 同时间段
- 同版本
- 补充材料
- 前后续版本
- 内容高度相关

## 15. 重复 PDF 检测

区分：

```text
exact         完全重复
near_duplicate 高度相似但有少量变化
related       同主题但并非重复
```

同主题不等于重复。

## 16. 版本识别

根据文件名、日期、版本号、正文变化和修改说明识别草稿、修订版、最终版等，并建立版本树。无法确定最新版本时明确标记不确定。

## 17. 文件重命名建议

默认只提出建议，不直接修改原文件。建议格式：

```text
[类别]_[主题]_[年份]_[版本]
```

例如：

```text
[课程资料]_大学英语六级阅读真题_2026.pdf
```

只有用户明确授权时才执行实际重命名。

## 18. 文件夹整理建议

默认生成建议结构，例如：

```text
PDF/
├── 学习资料/
│   ├── CET-4/
│   └── CET-6/
├── 论文/
│   ├── AI/
│   └── Computer Science/
├── 工作/
│   ├── 报告/
│   └── 会议/
└── 其他/
```

未经明确授权不得移动或删除原文件。

## 19. 智能总索引

批量 PDF 默认生成：

```text
| 文件 | 类型 | 主题 | 年份 | 页数 | 摘要 | 标签 |
|------|------|------|------|------|------|------|
```

需要时同时输出 CSV/JSON 等结构化索引。

## 20. 文档卡片

每份 PDF 默认生成：

```text
━━━━━━━━━━━━━━━━━━
📄 文档卡片
━━━━━━━━━━━━━━━━━━

文件：example.pdf
类型：学术论文
主题：Large Language Models
页数：18

一句话摘要：……

